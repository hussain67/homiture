export type SignupData = {
	email: string;
	password: string;
	username: string;
};

export type LoginData = {
	email: string;
	password: string;
};
export type User = {
	userName: string;
	jwt: string;
};
