export type SignupData = {
	email: string;
	password: string;
	username: string;
};

export type SigninData = {
	email: string;
	password: string;
};
export type User = {
	userName: string;
	jwt: string;
};
